---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: etcd-backup
  labels:
    app: etcd-infra
# Optional: if pulls from registry.redhat.io fail, copy the cluster pull secret:
#   oc extract secret/pull-secret -n openshift-config --to=/tmp --confirm
#   oc create secret generic redhat-pull-secret -n etcd-infra \
#     --from-file=.dockerconfigjson=/tmp/.dockerconfigjson \
#     --type=kubernetes.io/dockerconfigjson
# imagePullSecrets:
#   - name: redhat-pull-secret
